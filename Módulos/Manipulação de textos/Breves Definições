# Tratamento de cadeias de caracteres de texto 'string'
# Manipulando texto

    # Fatiamento ( pedaços de strings )
    # Análise ( len() = Análise de string, o comprimento da frase.
       #  frase = 'Curso em vídeo Python'
         #print(frase.len('o'))
    # Count('o')  - O módulo irá contar quantas vezes tem a letra 'o'.
            #frase = 'Curso em vídeo Python'
             #print(frase.count('o'))
    # frase.count('o',0,13) = irá contar quantas letras 'o' tem até o caractere 13
    # frase.find('deo') = encontra quantas vezes tem a sílaba 'deo'
    # Se colocar dentro da função 'find' uma string que nao existe o resultado será '-1'
    # 'in' operador de TRUE ou FALSE exemplo: 'curso' in frase - tem curso na frase?

    # TRANSFORMAÇÃO:
    #frase.replace('Python','Android') = trocar, reposicionar
           # frase = 'Curso em vídeo Python'
            #print = (frase.replace('Python','Android'))
    
    # essa função irá trocar a palavra 'python' da frase por 'android'

    #frase.upper()- troca e transforma uma string de minuscula em maiuscula.
            #frase = 'Curso em vídeo Python'
             #print(frase.upper().count('o'))
             #Resultado: Subi todas as letras 'o' e conta quantas tem
                     
            
    #frase.lower()- troca e transforma uma string maiuscula em minuscula
    #frase.capitalize() - joga todas as palavras em minusculo e somente a primeira maiusculo
    #frase.title() - as palavras seperadas por espaco irá colocar maiusculo
    #frase.strip() - Irá remover os espaços por fora, tanto do lado esquerdo/direito e ira manter espaços no meio
    #frase.rstrip() - Lado direito removido somente
    #frase.lstrip() - Remover espaços da esquerda e manter da direita dentro de palavras e strings

    # DIVISÃO: ( de strings )

        #frase.split() = refaz e separa em pedaços por listas
                #frase = 'Curso em vídeo Python'
                 #print(frase.split())

    # Junção:

        #'-'.join(frase) - irá juntar a cadeira de caracteres com o traço.
